import { expect, assert } from "chai";
import * as starknet from "starknet";

describe("Gas price modification", function () {
    it("should lower the price and allow tx execution", async function() {
        // start devnet

        // attempt tx - fails

        // lower price

        // attempt tx - succeeds
    });
});
